import { Preview } from "@/lib/preview/preview";

import { DemoSwitchField } from "./demo";

## Basic usage

<Preview>
  <DemoSwitchField />
</Preview>

```tsx
const Demo = () => {
  const [isChecked, setChecked] = useState(false);

  return (
    <SwitchField
      label="Use a default account"
      isChecked={isChecked}
      onChange={(event) => {
        setChecked(event.target.checked);
      }}
    />
  );
};
```

## Help text

The `helpText` prop lets you add more information about the switch field.

<Preview>
  <DemoSwitchField withHelpText />
</Preview>

```tsx
<SwitchField
  helpText="This default account will be used for all employees without dedicated accounts."
  {...otherProps}
/>
```
