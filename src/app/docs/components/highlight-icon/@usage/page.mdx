import { HighlightIcon } from "@dev-spendesk/grapes";
import { Preview } from "@/lib/preview/preview";

## Variant

The HighlightIcon comes with six visual styles: primary, alert, warning (default), info, success, and neutral.

<Preview>
  <HighlightIcon name="rotate" size="m" variant="primary" />
  <HighlightIcon name="failure" size="m" variant="alert" />
  <HighlightIcon name="warning" size="m" variant="warning" />
  <HighlightIcon name="info" size="m" variant="info" />
  <HighlightIcon name="success" size="m" variant="success" />
  <HighlightIcon name="closed-eye" size="m" variant="neutral" />
</Preview>
```tsx sandbox
<>
  <HighlightIcon name="rotate" size="m" variant="primary" />
  <HighlightIcon name="failure" size="m" variant="alert" />
  <HighlightIcon name="warning" size="m" variant="warning" />
  <HighlightIcon name="info" size="m" variant="info" />
  <HighlightIcon name="success" size="m" variant="success" />
  <HighlightIcon name="closed-eye" size="m" variant="neutral" />
</>
```

## Size

The HighlightIcon comes with three size: s, m and l (default).

<Preview>
  <HighlightIcon name="rotate" size="s" variant="primary" />
  <HighlightIcon name="rotate" size="m" variant="primary" />
  <HighlightIcon name="rotate" size="l" variant="primary" />
</Preview>
```tsx sandbox
<>
  <HighlightIcon name="rotate" size="s" variant="primary" />
  <HighlightIcon name="rotate" size="m" variant="primary" />
  <HighlightIcon name="rotate" size="l" variant="primary" />
</>
```
