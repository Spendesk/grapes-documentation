import { Preview } from "@/components/preview/preview";
import { DemoCalendarRange } from "./demo";

## Basic usage

<Preview>
  <DemoCalendarRange />
</Preview>

```tsx
const [value, setValue] = useState<DateRange>([
  new Date("2022-02-13"),
  new Date("2022-02-21"),
]);

return <CalendarRange value={value} onClick={setValue} />;
```

## With min and max date

When a minimum or maximum date is specified, these dates are grayed out and cannot be selected.

<Preview>
  <DemoCalendarRange
    maxDate={new Date("2022-02-24")}
    minDate={new Date("2022-02-04")}
  />
</Preview>

```tsx
const [value, setValue] = useState<DateRange>([
  new Date("2022-02-13"),
  new Date("2022-02-21"),
]);

return (
  <CalendarRange
    maxDate={new Date("2022-02-24")}
    minDate={new Date("2022-02-04")}
    value={value}
    onClick={setValue}
  />
);
```
