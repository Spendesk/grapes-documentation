## Summary

| Specification      | Value             |
| :----------------- | :---------------- |
| Aria Role          | `list`            |
| Accessibility name | prop `aria-label` |

:::info{title="Info"}
An alternative text label must be provided to identify the timeline. This should be added using the `aria-label` prop.
:::

#### TimelineItem

- Each item container has role `listitem`
- Each option has an accessible name given when prop `date`
