import { Callout } from "@dev-spendesk/grapes";

import { Preview } from "@/lib/preview/preview";
import { DemoRadioField } from "./demo";

<Callout
  className="mt-m"
  variant="warning"
  title="The RadioField should only be used within a RadioGroup"
>
  The RadioField component should be wrapped inside a RadioGroup. Learn more
  about the RadioGroup [here](/docs/components/radio-group).
</Callout>

## Basic usage

<Preview>
  <DemoRadioField />
</Preview>

```tsx sandbox
const Demo = () => {
  const [isChecked, setIsChecked] = useState(false);

  return (
    <RadioField
      name="approval"
      value="approval-needed"
      label="Approval Needed"
      isChecked={isChecked}
      onChange={(e) => {
        setIsChecked(true);
      }}
    />
  );
};
```

## Accessibility

Adheres to the [radio role requirements](https://www.w3.org/TR/wai-aria-1.2/#radio).

### Keyboard interactions

- `tab`: Moves focus on the element or outside
- `space`: Checks the focused radio button
