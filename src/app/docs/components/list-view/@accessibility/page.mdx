## Summary

| Specification      | Value             |
| :----------------- | :---------------- |
| Aria Role          | `list`            |
| Accessibility name | prop `aria-label` |

:::info{title="Info"}
An alternative text label must be provided to identify the list. This should be added using the `aria-label` prop.
:::

#### ListViewItem

- Each item container has role `listitem`
- Each option has an accessible name given when prop `children`
