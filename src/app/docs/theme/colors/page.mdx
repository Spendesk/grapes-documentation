import { MdxLayout } from "@/components/layout/layout";
import { ColorTokens } from "./color-tokens";
import { ColorTailwind } from "./color-tailwind";

export const metadata = {
  title: "Colors",
};

export default ({ children }) => (
  <MdxLayout metadata={metadata}>{children}</MdxLayout>
);

## Tokens

<ColorTokens />

## Usage in CSS

Whenever you want to apply this style, you can use the corresponding variable.

```CSS
.myClass {
    color: var(--color-primary);
}
```

## Usage with TailwindCSS

Whenever you want to use a color from Grapes with TailwindCSS, you can use the corresponding keys.

Example with text-color:

<ColorTailwind />

See documentation from TailwindCSS: [https://tailwindcss.com/docs/text-color](https://tailwindcss.com/docs/text-color).
